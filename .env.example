# Leave DATABASE_URL empty for demo mode.
DATABASE_URL=
APP_BASE_URL=http://localhost:3000
OWNER_NOTIFICATION_EMAIL=

# Optional sanitized issue destination.
GITHUB_ISSUE_REPO=

# Future adapters. Keep unset until configured and reviewed.
RUNTIME_STORE=
EMAIL_PROVIDER=
OBJECT_STORAGE_PROVIDER=

# Production auth and agent runtime. Required before using real renter data.
AUTH_PROVIDER=
OWNER_ADMIN_EMAIL=
MCP_SERVER_URL=
AGENT_RUNTIME_URL=

# Add model provider keys only in Vercel/Railway secret storage, not in this file.
